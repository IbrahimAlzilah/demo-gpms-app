# Types Directory

Global TypeScript type definitions.

## Naming Convention

Use `name.types.ts` pattern (e.g., `api.types.ts`, `common.types.ts`)

Feature-specific types should be placed in their respective feature folders under `features/{feature}/types/`.
