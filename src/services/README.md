# Services Directory

Global API services and service utilities.

## Naming Convention

Use `name.service.ts` pattern (e.g., `api.service.ts`)

Feature-specific services should be placed in their respective feature folders under `features/{feature}/api/`.
