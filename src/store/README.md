# Store Directory

Global Zustand stores for application-wide state management.

## Naming Convention

Use `name.store.ts` pattern (e.g., `app.store.ts`, `theme.store.ts`)

Feature-specific stores should be placed in their respective feature folders under `features/{feature}/store/`.
