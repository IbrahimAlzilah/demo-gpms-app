# Hooks Directory

Global reusable custom hooks.

## Naming Convention

Use `useName.ts` pattern (e.g., `useDebounce.ts`, `useLocalStorage.ts`)

These hooks are shared across multiple features and should be generic enough for reuse.

