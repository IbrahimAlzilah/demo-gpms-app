# App Directory

Application-level configuration and setup files.

## Structure

- **layouts/**: Layout components that wrap pages (MainLayout, AuthLayout, etc.)
- **providers/**: React context providers (QueryClient, Theme, etc.)
- **router/**: Routing configuration and route definitions
- **App.tsx**: Root App component that sets up providers and routing

