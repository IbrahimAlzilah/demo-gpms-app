# الواجهة الأمامية (Frontend) — توثيق مستقل

هذا المستند يشرح الـ Frontend **بشكل مستقل** عن الـ Backend. يمكن قراءته لفهم عمل الواجهة دون الحاجة لمعرفة الخادم بالتفصيل.

---

## 1. نظرة عامة على الـ Frontend

الواجهة الأمامية مبنية بـ **React** و **TypeScript** و **Vite**، وتقوم بـ:

- عرض واجهة المستخدم (صفحات، جداول، نماذج، أزرار)
- إرسال طلبات HTTP إلى الـ Backend عبر Axios
- حفظ التوكن والمستخدم في المتصفح
- إدارة التوجيه حسب الدور (Routing)
- عرض الإشعارات والرسائل
- دعم اللغة العربية (RTL) والثيم الفاتح/الغامق

---

## 2. التقنيات المستخدمة

| التقنية                   | الاستخدام                                          |
| ------------------------- | -------------------------------------------------- |
| **React**                 | بناء واجهة المستخدم                                |
| **TypeScript**            | كتابة الكود بأمان أنواع (type-safe)                |
| **Vite**                  | تجميع وتطوير المشروع بسرعة                         |
| **Tailwind CSS**          | التنسيق (CSS)                                      |
| **Radix UI**              | مكونات واجهة جاهزة (Dialog, Select, Dropdown، إلخ) |
| **React Router**          | التوجيه بين الصفحات                                |
| **TanStack Query**        | جلب وتحديث البيانات من الـ API                     |
| **Zustand**               | إدارة الحالة (Auth, Theme)                         |
| **Axios**                 | إرسال طلبات HTTP                                   |
| **React Hook Form + Zod** | التحقق من النماذج                                  |
| **i18next**               | الترجمة (العربية/الإنجليزية)                       |
| **Lucide React**          | الأيقونات                                          |

---

## 3. نمط تنظيم المشروع (Project Structure Pattern)

### النمط المستخدم: **Page/Feature-based** (الصفحات والميزات)

الواجهة منظمة بحيث:

- كل **ميزة** لها مجلد خاص (مثل `proposals`, `groups`, `evaluation`)
- داخل كل ميزة: مجلد `api` (خدمات API)، `components`، `hooks`، `list` أو `detail`
- مكونات عامة في `components/common` و `components/ui`
- التوجيه حسب الدور في `routes/`

### لماذا اختير هذا الهيكل؟

1. **سهولة التوسع**: إضافة ميزة جديدة تتم بإضافة مجلد جديد دون التأثير على بقية الكود
2. **وضوح المسؤولية**: كل ميزة تحتوي على كل ما تحتاجه (API، مكونات، hooks)
3. **إعادة الاستخدام**: مكونات `ui` و `common` تستخدم في صفحات متعددة
4. **تنظيم حسب الدور**: صفحات الطالب والمشرف واللجان منظمة في مجلدات منفصلة

### مزايا هذا الهيكل

- **سهولة التنقل**: معرفة أين يبحث عن ميزة معينة
- **مناسب للتطوير الجماعي**: مطورون مختلفون يمكنهم العمل على ميزات مختلفة
- **Lazy Loading**: تحميل الصفحات عند الحاجة لتقليل وقت التحميل الأولي

---

## 4. شرح أهم المجلدات والملفات (Key Folders and Files)

### 4.1 مجلد `src/`

| المجلد        | الوظيفة                                                              |
| ------------- | -------------------------------------------------------------------- |
| `pages/`      | الصفحات مقسمة حسب الدور: admin, auth, committee, student, supervisor |
| `components/` | مكونات مشتركة: common, layout, ui (شريط جانبي، هيدر، جداول، نماذج)   |
| `routes/`     | إعداد المسارات، التوجيه حسب الدور، الحماية                           |
| `lib/`        | axios، constants، utils، i18n                                        |
| `hooks/`      | Hooks مخصصة (useDebounce, useDataTable, usePeriodCheck)              |
| `context/`    | QueryProvider, I18nProvider, ThemeProvider, DirectionProvider        |
| `stores/`     | Zustand (auth.store, theme.store, app.store)                         |
| `services/`   | api.service، notifications.service                                   |
| `types/`      | تعريفات TypeScript                                                   |
| `layouts/`    | تخطيط الصفحات (شريط جانبي، محتوى)                                    |

### 4.2 هيكل ميزة نموذجية (مثال: `pages/student/proposals`)

```
proposals/
├── api/                    ← خدمات API (استدعاء Backend)
│   └── proposal.service.ts
├── components/             ← مكونات خاصة بالمقترحات
│   └── ProposalForm.tsx
├── hooks/                  ← منطق مخصص (جلب بيانات، معالجة)
│   └── useProposals.ts
├── list/                   ← عرض القائمة
│   ├── ProposalsList.screen.tsx
│   └── ProposalsList.hook.ts
├── submit/                 ← صفحة تقديم مقترح جديد
│   └── ProposalsSubmit.hook.ts
└── types/                  ← أنواع TypeScript للمقترحات
```

**مثال مبسط — كيف تستدعي الصفحة الـ API:**

```typescript
// في proposal.service.ts:
export const proposalService = {
  getList: () => apiClient.get<Proposal[]>("/student/proposals"),
  create: (data: CreateProposalInput) =>
    apiClient.post("/student/proposals", data),
};

// في ProposalsList.hook.ts:
const { data } = useQuery({
  queryKey: ["proposals"],
  queryFn: () => proposalService.getList(),
});
```

### 4.3 مجلد `components/`

| المجلد    | الوظيفة                                                                       |
| --------- | ----------------------------------------------------------------------------- |
| `common/` | مكونات مشتركة: LoadingSpinner, ConfirmDialog, EmptyState, ProtectedRoute، إلخ |
| `layout/` | شريط جانبي، هيدر، قائمة تنقل، إشعارات                                         |
| `ui/`     | مكونات واجهة أساسية (زر، إدخال، جدول، نافذة منبثقة، إلخ) من Radix UI          |

### 4.4 مجلد `lib/`

| الملف          | الوظيفة                                                         |
| -------------- | --------------------------------------------------------------- |
| `axios.ts`     | إعداد Axios (base URL، إضافة التوكن، معالجة الاستجابة والأخطاء) |
| `constants.ts` | ثوابت: مسارات التوجيه، قائمة القوائم، روابط API                 |
| `utils.ts`     | دوال مساعدة (تنسيق التواريخ، استخراج رسالة الخطأ من API)        |
| `i18n/`        | إعداد الترجمة (العربية/الإنجليزية)                              |

### 4.5 ملفات الدخول والإعداد

| الملف            | الوظيفة                                                   |
| ---------------- | --------------------------------------------------------- |
| `index.html`     | صفحة HTML الأساسية (تحتوي على `<div id="root">`)          |
| `main.tsx`       | نقطة دخول React (يحمّل App ويضعه في root)                 |
| `App.tsx`        | المكون الرئيسي (ThemeProvider, DirectionProvider, Router) |
| `vite.config.ts` | إعداد Vite (مسارات، alias، plugins)                       |
| `.env`           | متغيرات البيئة (VITE_API_BASE_URL)                        |
| `package.json`   | تعريف حزم JavaScript و dependencies                       |

### `pages/` حسب الدور

| المجلد                   | الصفحات                                                                            |
| ------------------------ | ---------------------------------------------------------------------------------- |
| `auth/login`             | تسجيل الدخول                                                                       |
| `auth/password-recovery` | استعادة كلمة المرور                                                                |
| `student/`               | لوحة التحكم، مقترحات، مشاريع، مجموعات، طلبات، مستندات، متابعة، درجات               |
| `supervisor/`            | لوحة التحكم، مقترحات، طلبات الإشراف، مشاريع، تقييم                                 |
| `committee/projects/`    | لجنة المشاريع: مقترحات، مشاريع، مشرفين، تسجيلات، طلبات، فترات، لجان، درجات، تقارير |
| `committee/discussion/`  | لجنة المناقشة: مشاريع، تقييم                                                       |
| `admin/`                 | لوحة التحكم، مستخدمون، إعدادات، تقارير                                             |

---

## تدفق الواجهة (UI Flow)

### 1. تسجيل الدخول

1. المستخدم يفتح `/login`
2. يدخل identifier و password
3. النموذج يرسل POST إلى `/api/auth/login`
4. عند النجاح، يحفظ التوكن والمستخدم في localStorage
5. يُعاد التوجيه إلى لوحة التحكم حسب الدور

### 2. التوجيه حسب الدور

- بعد تسجيل الدخول، يُعاد التوجيه إلى مسار افتراضي حسب الدور:
  - طالب → `/dashboard`
  - مشرف → `/dashboard`
  - لجنة المشاريع → `/dashboard`
  - لجنة المناقشة → `/dashboard`
  - مدير → `/dashboard`

### 3. شريط التنقل (Sidebar)

- يختلف حسب الدور.
- الطالب يرى: لوحة التحكم، مقترحات، مشاريع، مجموعات، طلبات، مستندات، متابعة، درجات.
- المشرف يرى: لوحة التحكم، مقترحات، طلبات الإشراف، مشاريع، تقييم.
- وهكذا لباقي الأدوار.

### 4. الصفحات الرئيسية

| الدور         | صفحات رئيسية                                                                                      |
| ------------- | ------------------------------------------------------------------------------------------------- |
| طالب          | مقترحاتي، مشاريع، مجموعات، رفع مستندات، طلبات، درجات                                              |
| مشرف          | مقترحاتي، طلبات الإشراف، مشاريعي، تقييم المشاريع                                                  |
| لجنة المشاريع | المقترحات، المشاريع، إعلان، تعيين مشرفين، التسجيلات، الطلبات، الفترات، التوزيع، الدرجات، التقارير |
| لجنة المناقشة | المشاريع المعينة، التقييم النهائي                                                                 |
| مدير          | المستخدمون، الإعدادات، التقارير                                                                   |

---

## الاتصال مع الـ Backend

### Axios

- ملف `src/lib/axios.ts` يُنشئ مثيل Axios باسم `apiClient`.
- العنوان الأساسي: `VITE_API_BASE_URL || "/api"`
- كل طلب يُضاف إليه رأس `Authorization: Bearer <token>`
- في الاستجابة، يتم استخراج `data` من هيكل `{ success, data, message }`

### خدمات API

- كل صفحة/ميزة لها مجلد `api/` يحتوي على ملفات خدمة مثل:
  - `auth.service.ts` — تسجيل الدخول والتسجيل
  - `proposal.service.ts` — المقترحات
  - `group.service.ts` — المجموعات
  - `project.service.ts` — المشاريع
- الخدمات تستخدم `apiClient` لإرسال الطلبات.

---

## إدارة الحالة

| الحالة    | المكان                    | الوظيفة                                    |
| --------- | ------------------------- | ------------------------------------------ |
| **Auth**  | `auth.store.ts` (Zustand) | المستخدم، التوكن، الصلاحيات                |
| **Theme** | `theme.store.ts`          | الوضع الفاتح/الغامق                        |
| **App**   | `app.store.ts`            | إعدادات عامة للتطبيق                       |
| **Data**  | TanStack Query            | البيانات من الـ API (مقترحات، مشاريع، إلخ) |

---

## التعامل مع تسجيل الدخول وحماية الصفحات

### ProtectedRouteWrapper

- يتحقق من وجود مستخدم مسجل.
- إذا لم يكن هناك مستخدم → يُعاد التوجيه إلى `/login`.

### RoleBasedRoutesWrapper

- يطابق المسار المطلوب مع المسارات المسموحة لدور المستخدم.
- إذا لم يكن المسار مسموحاً لدوره → يُعاد التوجيه إلى صفحة غير مصرح بها.

### auth.store

- `initialize()`: يتحقق من التوكن عبر `/api/auth/me`.
- عند 401: يُمسح التوكن ويُعاد التوجيه إلى تسجيل الدخول.
